import { NextPage } from 'next';
import { Container, Typography } from '@mui/material';

const Page: NextPage = () => {
  return (
    <Container maxWidth="lg" sx={{ mt: 2 }}>
      <Typography>Scorer</Typography>
    </Container>
  );
};

export default Page;
