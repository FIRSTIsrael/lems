import { Typography, Paper } from '@mui/material';

const AwardsPanel: React.FC = () => {
  return (
    <Paper sx={{ p: 2, textAlign: 'center' }}>
      <Typography variant="h1">בקרוב</Typography>
    </Paper>
  );
};

export default AwardsPanel;
