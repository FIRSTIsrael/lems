import { NextPage } from 'next';
import { Layout } from '../components/layout/layout';

const Page: NextPage = () => {
  return (
    <Layout>
      <p>Admin Dashboard</p>
    </Layout>
  );
};

export default Page;
