/**
 * LEMS GraphQL Schema
 *
 * This module provides the GraphQL schema definitions for the LEMS application.
 * The schema is shared between backend (Apollo Server) and frontend (Apollo Client code generation).
 *
 * @module @lems/types/api/lems/graphql
 */

export * from './schema';
