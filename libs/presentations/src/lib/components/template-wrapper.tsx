import { styled } from '@mui/system';

export const TemplateWrapper = styled('div')({
  position: 'absolute',
  top: 0,
  bottom: 0,
  left: 0,
  right: 0,
  pointerEvents: 'none'
});

export default TemplateWrapper;
